# Library: gis

Map tiles, elevation data, building footprints, floor plans. Large files gitignored.

See `data/library/CATALOG.md` for available datasets and `MANIFEST.json` for downloaded inventory.

**Disk budget:** See `.claude/agents/librarian-agent.md` for per-domain size limits.
